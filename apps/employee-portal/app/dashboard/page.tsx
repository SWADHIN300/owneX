import {redirect} from "next/navigation"; import {session} from "@/lib/session";
async function verify(wallet:string){const origin=process.env.PLATFORM_ORIGIN??"http://localhost:3000";const u=new URL("/api/roles/verify",origin);u.searchParams.set("wallet",wallet);u.searchParams.set("orgId",process.env.PORTAL_ORG_ID??"1");u.searchParams.set("app","employee-portal");const r=await fetch(u,{cache:"no-store"});return r.json()}
export default async function Dashboard(){const s=await session();if(!s.wallet)redirect("/");const v=await verify(s.wallet);if(!v.allowed)redirect(`/denied?reason=${v.reason}`);return <main className="wrap"><div className="eyebrow">Northwind Industries</div><h1>Welcome{v.displayName?`, ${v.displayName}`:""}.</h1><p className="muted">Your employee workspace, with access checked live by owneX.</p><div className="status"><div className="pill">{v.role}</div><h3>Live access check</h3><p className="muted">Verified at {new Date(v.verifiedAt).toLocaleString()} · Source: OwneX role verification</p><small>This app holds no keys and talks to no chain. A revocation or expiry appears on your next request.</small></div><div className="grid"><div className="card"><h3>Identity</h3><p>{v.jobTitle??"Team member"}</p><p className="muted">{v.department??"Northwind Industries"}</p></div><div className="card"><h3>Permissions</h3><p>{v.permissions?Object.entries(v.permissions).filter(([,x])=>x).map(([x])=>x.replaceAll("_"," ")).join(" · "):"Read-only access"}</p></div><div className="card"><h3>Membership</h3><p>{v.membership?.expired?"Expired":"Active"}</p><p className="muted">Joined {v.membership?.joinedAt?new Date(Number(v.membership.joinedAt)*1000).toLocaleDateString():"—"}</p></div></div></main>}
