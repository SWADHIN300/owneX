import {NextResponse} from "next/server"; import {randomBytes} from "node:crypto"; import {session} from "@/lib/session";
export async function GET(){const s=await session(); const state=randomBytes(24).toString("hex"); s.state=state;s.stateExpires=Date.now()+300000;await s.save(); const platform=process.env.PLATFORM_ORIGIN??"http://localhost:3000"; const callback=process.env.PORTAL_CALLBACK_URL??"http://localhost:3001/callback"; const u=new URL("/authorize",platform);u.searchParams.set("app","employee-portal");u.searchParams.set("redirect_uri",callback);u.searchParams.set("state",state);return NextResponse.redirect(u)}
