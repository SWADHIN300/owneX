import Link from "next/link";const messages:Record<string,string>={ORGANIZATION_NOT_FOUND:"That organization could not be found.",IDENTITY_NOT_REGISTERED:"This identity has not been registered with owneX.",IDENTITY_REVOKED:"This identity has been revoked. Ask an administrator to reactivate it.",ORGANIZATION_SUSPENDED:"This organization is currently suspended.",ROLE_EXPIRED:"Your role has expired. Ask an administrator to renew it.",NOT_A_MEMBER:"You are not a member of this organization.",APP_ACCESS_NOT_GRANTED:"Your role is valid, but this application is not enabled for it."}; export default async function Denied({searchParams}:{searchParams:Promise<{reason?:string}>}){const {reason}=await searchParams; return <main className="wrap"><div className="hero"><div className="eyebrow">Access decision</div><h1>We could not open your workspace.</h1><div className="error"><strong>{messages[reason??""]??"The authorization request was not completed."}</strong><p className="muted">Reason code: {reason??"UNKNOWN"}</p></div><Link className="button secondary" href="/">Return home</Link></div></main>}
